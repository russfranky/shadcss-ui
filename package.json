{
  "name": "shadcss-ui",
  "version": "0.0.1",
  "private": true,
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/www",
    "build": "npm run build --workspace=apps/www",
    "release": "npm run release --workspace=packages/shadcss"
  },
  "packageManager": "pnpm@9.0.0"
}
