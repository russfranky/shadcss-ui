{
  "name": "www",
  "version": "0.0.1",
  "private": true,
  "description": "shadcss/ui component showcase and docs.",
  "type": "module",
  "scripts": {
    "predev": "npm run build --workspace=packages/shadcss",
    "dev": "serve . -l 3333",
    "build": "npm run build --workspace=packages/shadcss"
  },
  "devDependencies": {
    "serve": "14.2.3"
  }
}
